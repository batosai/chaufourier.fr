@if ($props.object.lastPage > 1)
  <div class="pagination py-8 lg:py-16 w-full hidden sm:block">
    @!jrmc.pagination({ object: $props.object })
  </div>
  <div class="pagination py-8 lg:py-16 w-full text-center sm:hidden">
    @!jrmc.pagination.light({ object: $props.object })
  </div>
@end
